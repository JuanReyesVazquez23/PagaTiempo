services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pagatiempo
      POSTGRES_PASSWORD: pagatiempo
      POSTGRES_DB: pagatiempo
    ports:
      - "5432:5432"
    volumes:
      - pagatiempo_pg:/var/lib/postgresql/data

volumes:
  pagatiempo_pg:
